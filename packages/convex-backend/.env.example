# Written by `convex dev` after selecting a development deployment.
CONVEX_DEPLOYMENT=
CONVEX_URL=

# Convex deployment environment variables (set with `convex env`, not committed here).
CLERK_JWT_ISSUER_DOMAIN=
CLERK_M2M_AUDIENCE=mch_your-convex-receiver
BBPC_ENVIRONMENT=development
BBPC_API_VERSION=0.1.0
TMDB_API_KEY=replace-with-tmdb-api-key
